# Copy to .env and adjust. No spaces around "=" — Docker Compose's env_file
# does not trim them and would read the key as "API_PORT " with value " 4000".

# API
ENV=development
API_NAME=Example
API_HOST=127.0.0.1
API_PORT=4000

# Session tokens. Development fills in a well-known secret when this is unset.
# Production refuses to start without a unique value of at least 32 bytes.
# AUTH_TOKEN_SECRET=

# TLS is optional on loopback. Production listening on a non-loopback address
# requires both files, or a reverse proxy terminating TLS in front.
# TLS_CERT_FILE=
# TLS_KEY_FILE=

# Postgres
POSTGRES_PORT=5432
POSTGRES_HOST=localhost
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change-me
POSTGRES_DATABASE_NAME=postgres
POSTGRES_SSL_MODE=disable

# Connection pool. Keep idle equal to open unless you have a reason not to:
# a lower idle count makes the pool reopen connections under steady load.
DB_MAX_OPEN_CONNS=25
DB_MAX_IDLE_CONNS=25
