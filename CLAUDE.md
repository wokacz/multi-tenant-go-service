# CLAUDE.md

The working rules for this repository live in [`AGENTS.md`](AGENTS.md). Read it first.

It is the single instruction file on purpose. This one used to hold its own copy of the same rules, and the two had
already drifted — the duplicate pointed at a test file that does not exist at that path. Two files that must agree
eventually do not, and the one nobody is reading is the one that goes stale.

Detailed documentation is in [`docs/`](docs/README.md), in Polish. Code, identifiers and comments are in English.
