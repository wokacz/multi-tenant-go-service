# Auto-discovery pointer. Compose only looks for this filename in the working
# directory; the services live in .docker/compose.yml.
#
# project_directory is the repository root so relative paths in the included
# file (build context, .env, bind mounts) keep meaning "here", not ".docker/".
include:
  - path: .docker/compose.yml
    project_directory: .
