// @ts-nocheck
/* eslint-disable */
/* @noformat */
/* @formatter:off */
/**
 * Generated by ng-openapi
 * Generated Angular service for Devices controller
 * Do not edit this file manually
 */
import {HttpClient, HttpContext, HttpContextToken, HttpEvent, HttpHeaders, HttpParams, HttpResponse, } from '@angular/common/http';
import {inject, Injectable} from '@angular/core';
import {Observable} from 'rxjs';
import {BASE_PATH_DEFAULT, CLIENT_CONTEXT_TOKEN_DEFAULT} from '../tokens';
import {HttpParamsBuilder} from '../utils/http-params-builder';
import {ListDevicesOutputBody, ListLoginEventsOutputBody, RequestOptions} from '../models';

@Injectable({ providedIn: 'root' })
export class DevicesService {
  private readonly httpClient: HttpClient = inject(HttpClient);
  private readonly basePath: string = inject(BASE_PATH_DEFAULT);
  private readonly clientContextToken: HttpContextToken<string> = CLIENT_CONTEXT_TOKEN_DEFAULT;

  private createContextWithClientId(existingContext?: HttpContext): HttpContext {
    const context = existingContext || new HttpContext();
    return context.set(this.clientContextToken, 'default');
  }

  listDevices(
    observe?: 'body',
    options?: RequestOptions<'json'>,
  ): Observable<ListDevicesOutputBody>;
  listDevices(
    observe?: 'response',
    options?: RequestOptions<'json'>,
  ): Observable<HttpResponse<ListDevicesOutputBody>>;
  listDevices(
    observe?: 'events',
    options?: RequestOptions<'json'>,
  ): Observable<HttpEvent<ListDevicesOutputBody>>;
  /** Returns every device that has signed in to the account, most recently seen first, including revoked ones so the owner can see what was blocked. */
  listDevices(
    observe?: 'body' | 'events' | 'response',
    options?: RequestOptions<'arraybuffer' | 'blob' | 'json' | 'text'>,
  ): Observable<any> {
    const url = `${this.basePath}/v1/me/devices`;

    let headers: HttpHeaders;
    if (options?.headers instanceof HttpHeaders) {
      headers = options.headers;
    } else {
      headers = new HttpHeaders(options?.headers);
    }
    // Advertise the response content type declared in the spec
    if (!headers.has('Accept')) {
      headers = headers.set('Accept', 'application/json');
    }

    return this.httpClient.request('get', url, {
      observe,
      headers,
      reportProgress: options?.reportProgress,
      withCredentials: options?.withCredentials,
      context: this.createContextWithClientId(options?.context),
    });
  }

  revokeDevice(id: string, observe?: 'body', options?: RequestOptions<'json'>): Observable<any>;
  revokeDevice(
    id: string,
    observe?: 'response',
    options?: RequestOptions<'json'>,
  ): Observable<HttpResponse<any>>;
  revokeDevice(
    id: string,
    observe?: 'events',
    options?: RequestOptions<'json'>,
  ): Observable<HttpEvent<any>>;
  /** Blocks the device and drops its trust. Tokens already issued to it stop working on the next request rather than at expiry. Revoking the device making the call is allowed, and is how a client signs itself out. Revoking twice succeeds. An id that is not the caller's is 404. */
  revokeDevice(
    id: string,
    observe?: 'body' | 'events' | 'response',
    options?: RequestOptions<'arraybuffer' | 'blob' | 'json' | 'text'>,
  ): Observable<any> {
    const url = `${this.basePath}/v1/me/devices/${id}`;

    let headers: HttpHeaders;
    if (options?.headers instanceof HttpHeaders) {
      headers = options.headers;
    } else {
      headers = new HttpHeaders(options?.headers);
    }

    return this.httpClient.request('delete', url, {
      observe,
      headers,
      reportProgress: options?.reportProgress,
      withCredentials: options?.withCredentials,
      context: this.createContextWithClientId(options?.context),
    });
  }

  listLoginEvents(
    limit?: number,
    observe?: 'body',
    options?: RequestOptions<'json'>,
  ): Observable<ListLoginEventsOutputBody>;
  listLoginEvents(
    limit?: number,
    observe?: 'response',
    options?: RequestOptions<'json'>,
  ): Observable<HttpResponse<ListLoginEventsOutputBody>>;
  listLoginEvents(
    limit?: number,
    observe?: 'events',
    options?: RequestOptions<'json'>,
  ): Observable<HttpEvent<ListLoginEventsOutputBody>>;
  /** Returns the account's recent sign-in attempts, newest first. Attempts against an address that is not registered are not recorded, so an empty history means nobody has tried this account. */
  listLoginEvents(
    limit?: number,
    observe?: 'body' | 'events' | 'response',
    options?: RequestOptions<'arraybuffer' | 'blob' | 'json' | 'text'>,
  ): Observable<any> {
    const url = `${this.basePath}/v1/me/login-events`;

    let params = new HttpParams();
    if (limit != null) {
      params = HttpParamsBuilder.addToHttpParams(params, limit, 'limit');
    }

    let headers: HttpHeaders;
    if (options?.headers instanceof HttpHeaders) {
      headers = options.headers;
    } else {
      headers = new HttpHeaders(options?.headers);
    }
    // Advertise the response content type declared in the spec
    if (!headers.has('Accept')) {
      headers = headers.set('Accept', 'application/json');
    }

    return this.httpClient.request('get', url, {
      observe,
      headers,
      params,
      reportProgress: options?.reportProgress,
      withCredentials: options?.withCredentials,
      context: this.createContextWithClientId(options?.context),
    });
  }
}
