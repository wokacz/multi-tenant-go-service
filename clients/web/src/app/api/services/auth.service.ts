// @ts-nocheck
/* eslint-disable */
/* @noformat */
/* @formatter:off */
/**
 * Generated by ng-openapi
 * Generated Angular service for Auth controller
 * Do not edit this file manually
 */
import {
  HttpClient,
  HttpContext,
  HttpContextToken,
  HttpEvent,
  HttpHeaders,
  HttpParams,
  HttpResponse,
} from '@angular/common/http';
import { inject, Injectable } from '@angular/core';
import { Observable } from 'rxjs';
import { BASE_PATH_DEFAULT, CLIENT_CONTEXT_TOKEN_DEFAULT } from '../tokens';
import { HttpParamsBuilder } from '../utils/http-params-builder';
import {
  SetTwoFactorRequest,
  RequestOptions,
  RequestPasswordResetRequest,
  ConfirmPasswordResetRequest,
  CreateSessionRequest,
  SessionResponse,
  VerifySessionRequest,
  CreateUserRequest,
} from '../models';

@Injectable({ providedIn: 'root' })
export class AuthService {
  private readonly httpClient: HttpClient = inject(HttpClient);
  private readonly basePath: string = inject(BASE_PATH_DEFAULT);
  private readonly clientContextToken: HttpContextToken<string> = CLIENT_CONTEXT_TOKEN_DEFAULT;

  private createContextWithClientId(existingContext?: HttpContext): HttpContext {
    const context = existingContext || new HttpContext();
    return context.set(this.clientContextToken, 'default');
  }

  setTwoFactor(
    setTwoFactorRequest: SetTwoFactorRequest,
    observe?: 'body',
    options?: RequestOptions<'json'>,
  ): Observable<any>;
  setTwoFactor(
    setTwoFactorRequest: SetTwoFactorRequest,
    observe?: 'response',
    options?: RequestOptions<'json'>,
  ): Observable<HttpResponse<any>>;
  setTwoFactor(
    setTwoFactorRequest: SetTwoFactorRequest,
    observe?: 'events',
    options?: RequestOptions<'json'>,
  ): Observable<HttpEvent<any>>;
  /** Requires the current password as well as the token. Enabling also trusts the calling device, so an account whose address no longer receives mail is not locked out by its own setting; every other device will be asked for a code on its next sign-in. */
  setTwoFactor(
    setTwoFactorRequest: SetTwoFactorRequest,
    observe?: 'body' | 'events' | 'response',
    options?: RequestOptions<'arraybuffer' | 'blob' | 'json' | 'text'>,
  ): Observable<any> {
    const url = `${this.basePath}/v1/me/two-factor`;

    let headers: HttpHeaders;
    if (options?.headers instanceof HttpHeaders) {
      headers = options.headers;
    } else {
      headers = new HttpHeaders(options?.headers);
    }
    // Set Content-Type for JSON requests if not already set
    if (!headers.has('Content-Type')) {
      headers = headers.set('Content-Type', 'application/json');
    }

    return this.httpClient.request('put', url, {
      body: setTwoFactorRequest,
      observe,
      headers,
      reportProgress: options?.reportProgress,
      withCredentials: options?.withCredentials,
      context: this.createContextWithClientId(options?.context),
    });
  }

  requestPasswordReset(
    requestPasswordResetRequest: RequestPasswordResetRequest,
    observe?: 'body',
    options?: RequestOptions<'json'>,
  ): Observable<any>;
  requestPasswordReset(
    requestPasswordResetRequest: RequestPasswordResetRequest,
    observe?: 'response',
    options?: RequestOptions<'json'>,
  ): Observable<HttpResponse<any>>;
  requestPasswordReset(
    requestPasswordResetRequest: RequestPasswordResetRequest,
    observe?: 'events',
    options?: RequestOptions<'json'>,
  ): Observable<HttpEvent<any>>;
  /** Sends a one-time code to the address if it is registered. The response is always 204 — including when delivery or storage fails — so the call cannot be used to discover accounts. In development without SMTP the code is written to the process log. */
  requestPasswordReset(
    requestPasswordResetRequest: RequestPasswordResetRequest,
    observe?: 'body' | 'events' | 'response',
    options?: RequestOptions<'arraybuffer' | 'blob' | 'json' | 'text'>,
  ): Observable<any> {
    const url = `${this.basePath}/v1/password-resets`;

    let headers: HttpHeaders;
    if (options?.headers instanceof HttpHeaders) {
      headers = options.headers;
    } else {
      headers = new HttpHeaders(options?.headers);
    }
    // Set Content-Type for JSON requests if not already set
    if (!headers.has('Content-Type')) {
      headers = headers.set('Content-Type', 'application/json');
    }

    return this.httpClient.request('post', url, {
      body: requestPasswordResetRequest,
      observe,
      headers,
      reportProgress: options?.reportProgress,
      withCredentials: options?.withCredentials,
      context: this.createContextWithClientId(options?.context),
    });
  }

  confirmPasswordReset(
    confirmPasswordResetRequest: ConfirmPasswordResetRequest,
    observe?: 'body',
    options?: RequestOptions<'json'>,
  ): Observable<any>;
  confirmPasswordReset(
    confirmPasswordResetRequest: ConfirmPasswordResetRequest,
    observe?: 'response',
    options?: RequestOptions<'json'>,
  ): Observable<HttpResponse<any>>;
  confirmPasswordReset(
    confirmPasswordResetRequest: ConfirmPasswordResetRequest,
    observe?: 'events',
    options?: RequestOptions<'json'>,
  ): Observable<HttpEvent<any>>;
  /** Spends the code and sets a new password. password and password_confirm must match. Unknown addresses, expired codes and wrong codes share one error. */
  confirmPasswordReset(
    confirmPasswordResetRequest: ConfirmPasswordResetRequest,
    observe?: 'body' | 'events' | 'response',
    options?: RequestOptions<'arraybuffer' | 'blob' | 'json' | 'text'>,
  ): Observable<any> {
    const url = `${this.basePath}/v1/password-resets/confirm`;

    let headers: HttpHeaders;
    if (options?.headers instanceof HttpHeaders) {
      headers = options.headers;
    } else {
      headers = new HttpHeaders(options?.headers);
    }
    // Set Content-Type for JSON requests if not already set
    if (!headers.has('Content-Type')) {
      headers = headers.set('Content-Type', 'application/json');
    }

    return this.httpClient.request('post', url, {
      body: confirmPasswordResetRequest,
      observe,
      headers,
      reportProgress: options?.reportProgress,
      withCredentials: options?.withCredentials,
      context: this.createContextWithClientId(options?.context),
    });
  }

  createSession(
    createSessionRequest: CreateSessionRequest,
    observe?: 'body',
    options?: RequestOptions<'json'>,
  ): Observable<SessionResponse>;
  createSession(
    createSessionRequest: CreateSessionRequest,
    observe?: 'response',
    options?: RequestOptions<'json'>,
  ): Observable<HttpResponse<SessionResponse>>;
  createSession(
    createSessionRequest: CreateSessionRequest,
    observe?: 'events',
    options?: RequestOptions<'json'>,
  ): Observable<HttpEvent<SessionResponse>>;
  /** Exchanges an email and password for a Bearer token. Wrong passwords and unknown addresses share one error. When the account has two-factor on and the X-Device-Token is missing or names an untrusted device, the answer is 202 with two_factor_required and a code goes out by email; finish at POST /v1/sessions/verify. */
  createSession(
    createSessionRequest: CreateSessionRequest,
    observe?: 'body' | 'events' | 'response',
    options?: RequestOptions<'arraybuffer' | 'blob' | 'json' | 'text'>,
  ): Observable<any> {
    const url = `${this.basePath}/v1/sessions`;

    let headers: HttpHeaders;
    if (options?.headers instanceof HttpHeaders) {
      headers = options.headers;
    } else {
      headers = new HttpHeaders(options?.headers);
    }
    // Advertise the response content type declared in the spec
    if (!headers.has('Accept')) {
      headers = headers.set('Accept', 'application/json');
    }
    // Set Content-Type for JSON requests if not already set
    if (!headers.has('Content-Type')) {
      headers = headers.set('Content-Type', 'application/json');
    }

    return this.httpClient.request('post', url, {
      body: createSessionRequest,
      observe,
      headers,
      reportProgress: options?.reportProgress,
      withCredentials: options?.withCredentials,
      context: this.createContextWithClientId(options?.context),
    });
  }

  verifySession(
    verifySessionRequest: VerifySessionRequest,
    observe?: 'body',
    options?: RequestOptions<'json'>,
  ): Observable<SessionResponse>;
  verifySession(
    verifySessionRequest: VerifySessionRequest,
    observe?: 'response',
    options?: RequestOptions<'json'>,
  ): Observable<HttpResponse<SessionResponse>>;
  verifySession(
    verifySessionRequest: VerifySessionRequest,
    observe?: 'events',
    options?: RequestOptions<'json'>,
  ): Observable<HttpEvent<SessionResponse>>;
  /** Spends the emailed code and issues the token. The X-Device-Token must be the one returned by the sign-in that raised the challenge — a code authorises one device, not one mailbox. Succeeding also trusts that device, so later sign-ins from it skip the code. Every way of getting it wrong shares one error. */
  verifySession(
    verifySessionRequest: VerifySessionRequest,
    observe?: 'body' | 'events' | 'response',
    options?: RequestOptions<'arraybuffer' | 'blob' | 'json' | 'text'>,
  ): Observable<any> {
    const url = `${this.basePath}/v1/sessions/verify`;

    let headers: HttpHeaders;
    if (options?.headers instanceof HttpHeaders) {
      headers = options.headers;
    } else {
      headers = new HttpHeaders(options?.headers);
    }
    // Advertise the response content type declared in the spec
    if (!headers.has('Accept')) {
      headers = headers.set('Accept', 'application/json');
    }
    // Set Content-Type for JSON requests if not already set
    if (!headers.has('Content-Type')) {
      headers = headers.set('Content-Type', 'application/json');
    }

    return this.httpClient.request('post', url, {
      body: verifySessionRequest,
      observe,
      headers,
      reportProgress: options?.reportProgress,
      withCredentials: options?.withCredentials,
      context: this.createContextWithClientId(options?.context),
    });
  }

  createUser(
    createUserRequest: CreateUserRequest,
    observe?: 'body',
    options?: RequestOptions<'json'>,
  ): Observable<any>;
  createUser(
    createUserRequest: CreateUserRequest,
    observe?: 'response',
    options?: RequestOptions<'json'>,
  ): Observable<HttpResponse<any>>;
  createUser(
    createUserRequest: CreateUserRequest,
    observe?: 'events',
    options?: RequestOptions<'json'>,
  ): Observable<HttpEvent<any>>;
  /** Creates an account. password and password_confirm must match. A duplicate email is reported as success so the response cannot be used to discover registered addresses. Sign in with POST /v1/sessions to obtain a token. */
  createUser(
    createUserRequest: CreateUserRequest,
    observe?: 'body' | 'events' | 'response',
    options?: RequestOptions<'arraybuffer' | 'blob' | 'json' | 'text'>,
  ): Observable<any> {
    const url = `${this.basePath}/v1/users`;

    let headers: HttpHeaders;
    if (options?.headers instanceof HttpHeaders) {
      headers = options.headers;
    } else {
      headers = new HttpHeaders(options?.headers);
    }
    // Set Content-Type for JSON requests if not already set
    if (!headers.has('Content-Type')) {
      headers = headers.set('Content-Type', 'application/json');
    }

    return this.httpClient.request('post', url, {
      body: createUserRequest,
      observe,
      headers,
      reportProgress: options?.reportProgress,
      withCredentials: options?.withCredentials,
      context: this.createContextWithClientId(options?.context),
    });
  }
}
