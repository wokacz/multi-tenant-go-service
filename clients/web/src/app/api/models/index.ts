// @ts-nocheck
/* eslint-disable */
/* @noformat */
/* @formatter:off */
/**
 * Generated by ng-openapi
 * Generated TypeScript interfaces from Swagger specification
 * Do not edit this file manually
 */
import { HttpContext, HttpHeaders } from '@angular/common/http';
export interface AddMemberRequest {
  /** A URL to the JSON Schema for this object. */
  readonly $schema?: string;
  /** Address to invite. An existing account is not required. */
  email: string;
  /** Roles to grant. Every one must be a role the caller could grant themselves. */
  role_ids: Array<string>;
}

export interface AppointOwnerRequest {
  /** A URL to the JSON Schema for this object. */
  readonly $schema?: string;
  /** Account to make owner */
  user_id: string;
}

export interface AuditEventResponse {
  /** What changed, e.g. member.roles_changed */
  action: string;
  /** Who made the change */
  actor: AuditParty;
  /** When the change was made */
  at: Date;
  detail?: string;
  id: string;
  ip?: string;
  organization_id?: string;
  role_id?: string;
  /** Captured when the change was made, so it survives the role being deleted */
  role_key?: string;
  /** Who it was about, absent for changes that are not about a person */
  subject?: AuditParty;
  user_agent?: string;
}

export interface AuditParty {
  email?: string;
  id: string;
  name?: string;
}

export interface BeginEmailChangeRequest {
  /** A URL to the JSON Schema for this object. */
  readonly $schema?: string;
  /** Address to move the account to */
  new_email: string;
  /** Current password */
  password: string;
}

export interface ChangePasswordRequest {
  /** A URL to the JSON Schema for this object. */
  readonly $schema?: string;
  /** Current password */
  current_password: string;
  /** New password */
  password: string;
  /** New password again */
  password_confirm: string;
}

export interface ConfirmEmailChangeRequest {
  /** A URL to the JSON Schema for this object. */
  readonly $schema?: string;
  /** Code from the message sent to the new address */
  code: string;
}

export interface ConfirmPasswordResetRequest {
  /** A URL to the JSON Schema for this object. */
  readonly $schema?: string;
  /** The code delivered after requesting a reset */
  code: string;
  /** Email address */
  email: string;
  /** New password */
  password: string;
  /** Must match password */
  password_confirm: string;
}

export interface CreatePlatformOrganizationRequest {
  /** A URL to the JSON Schema for this object. */
  readonly $schema?: string;
  /** Display name */
  name: string;
  /** Lowercase handle, unique in the installation */
  slug: string;
}

export interface CreateRoleRequest {
  /** A URL to the JSON Schema for this object. */
  readonly $schema?: string;
  description?: string;
  /** Lowercase identifier, unique in the organization */
  key: string;
  /** Display name */
  name: string;
  /** Permission keys. Every one must be a permission the caller holds. */
  permissions: Array<string>;
}

export interface CreateSessionRequest {
  /** A URL to the JSON Schema for this object. */
  readonly $schema?: string;
  /** Email address */
  email: string;
  /** Plain-text password */
  password: string;
}

export interface CreateUserRequest {
  /** A URL to the JSON Schema for this object. */
  readonly $schema?: string;
  /** Email address */
  email: string;
  /** Display name */
  name: string;
  /** Plain-text password, hashed before storage */
  password: string;
  /** Must match password */
  password_confirm: string;
}

export interface DeviceResponse {
  /** When it was first seen */
  created_at: Date;
  /** Whether this is the device making the request */
  current: boolean;
  /** Unique identifier */
  id: string;
  /** Name the owner gave this device */
  label?: string;
  /** Address last seen using it */
  last_ip?: string;
  /** When it was last used, absent if never */
  last_seen_at?: Date;
  /** Whether it is blocked from signing in */
  revoked: boolean;
  /** Whether it may skip the second factor */
  trusted: boolean;
  /** User agent last seen using it */
  user_agent?: string;
}

export interface Document {
  /** A URL to the JSON Schema for this object. */
  readonly $schema?: string;
  /** Stable machine-readable identifier for this refusal */
  code?: string;
  /** Human-readable explanation, in the negotiated language. Never parse this — use code. */
  detail?: string;
  /** Field-level problems, when the request failed validation */
  errors?: Array<ErrorDetail>;
  /** A URI identifying this occurrence */
  instance?: string;
  /** Permission key the caller was missing */
  required_permission?: string;
  /** HTTP status code */
  status?: number;
  /** Short, human-readable summary of the problem type */
  title?: string;
  /** A URI identifying the problem type */
  type?: string;
}

export interface ErrorDetail {
  /** Where the error occurred, e.g. 'body.items[3].tags' or 'path.thing-id' */
  location?: string;
  /** Error message text */
  message?: string;
  /** The value at the given location */
  value?: any;
}

export interface GrantSystemRoleRequest {
  /** A URL to the JSON Schema for this object. */
  readonly $schema?: string;
  /** Installation-wide role key */
  role_key: string;
  /** Account to grant it to */
  user_id: string;
}

export interface HealthOutputBody {
  /** A URL to the JSON Schema for this object. */
  readonly $schema?: string;
  /** Overall health of the service */
  status: string;
}

export interface InvitationResponse {
  /** A URL to the JSON Schema for this object. */
  readonly $schema?: string;
  /** Address the invitation was issued to */
  email: string;
  /** After this it can no longer be accepted */
  expires_at: Date;
  organization: OrganizationResponse;
  /** Role keys it will grant once accepted */
  roles: Array<string>;
}

export interface InvitationTokenRequest {
  /** A URL to the JSON Schema for this object. */
  readonly $schema?: string;
  /** Token from the invitation message */
  token: string;
}

export interface ListAuditOutputBody {
  /** A URL to the JSON Schema for this object. */
  readonly $schema?: string;
  events: Array<AuditEventResponse>;
}

export interface ListDevicesOutputBody {
  /** A URL to the JSON Schema for this object. */
  readonly $schema?: string;
  devices: Array<DeviceResponse>;
}

export interface ListInvitationsOutputBody {
  /** A URL to the JSON Schema for this object. */
  readonly $schema?: string;
  invitations: Array<InvitationResponse>;
}

export interface ListLoginEventsOutputBody {
  /** A URL to the JSON Schema for this object. */
  readonly $schema?: string;
  events: Array<LoginEventResponse>;
}

export interface ListMembersOutputBody {
  /** A URL to the JSON Schema for this object. */
  readonly $schema?: string;
  members: Array<MemberResponse>;
}

export interface ListMyInvitationsOutputBody {
  /** A URL to the JSON Schema for this object. */
  readonly $schema?: string;
  invitations: Array<InvitationResponse>;
}

export interface ListMyOrganizationsOutputBody {
  /** A URL to the JSON Schema for this object. */
  readonly $schema?: string;
  organizations: Array<MembershipResponse>;
}

export interface ListPermissionsOutputBody {
  /** A URL to the JSON Schema for this object. */
  readonly $schema?: string;
  permissions: Array<PermissionResponse>;
}

export interface ListPlatformOrganizationsOutputBody {
  /** A URL to the JSON Schema for this object. */
  readonly $schema?: string;
  organizations: Array<PlatformOrganizationResponse>;
}

export interface ListPlatformUsersOutputBody {
  /** A URL to the JSON Schema for this object. */
  readonly $schema?: string;
  users: Array<PlatformUserResponse>;
}

export interface ListRolesOutputBody {
  /** A URL to the JSON Schema for this object. */
  readonly $schema?: string;
  roles: Array<RoleResponse>;
}

export interface ListSystemRolesOutputBody {
  /** A URL to the JSON Schema for this object. */
  readonly $schema?: string;
  holders: Array<SystemRoleHolderResponse>;
}

export interface LoginEventResponse {
  /** When it happened */
  created_at: Date;
  /** Device involved, absent when the attempt failed before one was identified */
  device_id?: string;
  /** Unique identifier */
  id: string;
  /** Address the attempt came from */
  ip: string;
  /** How the attempt ended */
  outcome: 'success' | 'bad_password' | 'mfa_failed' | 'locked';
  /** User agent that made the attempt */
  user_agent?: string;
}

export interface MemberResponse {
  /** A URL to the JSON Schema for this object. */
  readonly $schema?: string;
  /** Email address */
  email: string;
  /** Membership id, used to address this person here */
  id: string;
  /** When the membership first became active */
  joined_at?: Date;
  /** Display name. Absent until the invitation is accepted. */
  name?: string;
  /** Roles held in this organization */
  roles: Array<RoleSummaryResponse>;
  /** Whether the membership grants anything */
  status: 'active' | 'suspended';
  /** The account behind the membership. Absent while the invitation is outstanding, so listing members cannot tell a registered address from an unknown one. */
  user_id?: string;
}

export interface MembershipResponse {
  /** Membership id, which is what DELETE /v1/me/memberships/{membershipID} takes to leave */
  id: string;
  organization: OrganizationResponse;
  /** Keys of the roles the caller holds here */
  roles: Array<string>;
  /** Where the caller stands in this organization */
  status: 'active' | 'suspended';
}

export interface OrganizationResponse {
  /** A URL to the JSON Schema for this object. */
  readonly $schema?: string;
  /** When the organization was created */
  created_at: Date;
  /** Unique identifier */
  id: string;
  /** Display name */
  name: string;
  /** Stable human-readable handle */
  slug: string;
}

export interface PermissionResponse {
  /** Heading to file it under */
  group: string;
  /** Stable identifier used everywhere else in the API */
  key: string;
  /** What the permission is measured against */
  scope: 'organization' | 'system';
}

export interface PermissionSnapshotResponse {
  /** A URL to the JSON Schema for this object. */
  readonly $schema?: string;
  organizations: Array<SnapshotMembership>;
  system: SnapshotSystem;
  user: SnapshotUser;
}

export interface PlatformOrganizationResponse {
  /** A URL to the JSON Schema for this object. */
  readonly $schema?: string;
  /** When the organization was created */
  created_at: Date;
  /** Unique identifier */
  id: string;
  /** Refuses deletion. The default organization carries it. */
  is_protected: boolean;
  /** Display name */
  name: string;
  /** Stable human-readable handle */
  slug: string;
}

export interface PlatformUserResponse {
  created_at: string;
  email: string;
  id: string;
  locale?: string;
  name: string;
  /** Blocked from signing in anywhere */
  suspended: boolean;
  suspended_at?: string;
}

export interface RequestPasswordResetRequest {
  /** A URL to the JSON Schema for this object. */
  readonly $schema?: string;
  /** Email address */
  email: string;
}

export interface RoleResponse {
  /** A URL to the JSON Schema for this object. */
  readonly $schema?: string;
  description?: string;
  id: string;
  /** Ships with the product: visible and clonable, never editable */
  is_system: boolean;
  /** Stable identifier, and the translation key */
  key: string;
  /** How many people hold it */
  members: number;
  /** Display name */
  name: string;
  /** Permission keys this role grants */
  permissions: Array<string>;
}

export interface RoleSummaryResponse {
  id: string;
  /** Whether the role ships with the product and cannot be edited */
  is_system: boolean;
  /** Stable identifier, and the translation key */
  key: string;
  /** Display name, already localised */
  name: string;
}

export interface SessionResponse {
  /** A URL to the JSON Schema for this object. */
  readonly $schema?: string;
  /** Opaque secret identifying this device on later sign-ins */
  device_token?: string;
  /** When the token stops being accepted */
  expires_at?: Date;
  /** Bearer token for subsequent requests */
  token?: string;
  /** True when a code was emailed and no token was issued */
  two_factor_required: boolean;
  user?: UserResponse;
}

export interface SetMemberRolesRequest {
  /** A URL to the JSON Schema for this object. */
  readonly $schema?: string;
  /** The complete set of roles this person should hold */
  role_ids: Array<string>;
}

export interface SetRolePermissionsRequest {
  /** A URL to the JSON Schema for this object. */
  readonly $schema?: string;
  /** The complete set of permission keys this role should grant */
  permissions: Array<string>;
}

export interface SetTwoFactorRequest {
  /** A URL to the JSON Schema for this object. */
  readonly $schema?: string;
  /** Whether sign-in from an untrusted device should need a code */
  enabled: boolean;
  /** Current password */
  password: string;
}

export interface SnapshotMembership {
  id: string;
  name: string;
  /** Permission keys held here */
  permissions: Array<string>;
  /** Role keys held here */
  roles: Array<string>;
  slug: string;
  status: 'active' | 'suspended';
}

export interface SnapshotSystem {
  /** Installation-wide permission keys */
  permissions: Array<string>;
  /** Installation-wide role keys, empty for almost everyone */
  roles: Array<string>;
}

export interface SnapshotUser {
  id: string;
  /** Language this response was written in */
  locale: string;
}

export interface SuspendPlatformUserRequest {
  /** A URL to the JSON Schema for this object. */
  readonly $schema?: string;
  /** true blocks the account everywhere, false restores it */
  suspended: boolean;
}

export interface SystemRoleHolderResponse {
  email?: string;
  granted_at: Date;
  /** Who granted it, absent when it came from the bootstrap command */
  granted_by?: string;
  name?: string;
  /** Installation-wide role held, e.g. platform_admin */
  role_key: string;
  user_id: string;
}

export interface UpdateMeRequest {
  /** A URL to the JSON Schema for this object. */
  readonly $schema?: string;
  /** BCP 47 tag, or empty to negotiate from Accept-Language on every request */
  locale?: string;
  /** Display name */
  name?: string;
}

export interface UpdateMemberStatusRequest {
  /** A URL to the JSON Schema for this object. */
  readonly $schema?: string;
  /** Suspending withdraws every permission without losing the role assignments */
  status: 'active' | 'suspended';
}

export interface UpdateOrganizationRequest {
  /** A URL to the JSON Schema for this object. */
  readonly $schema?: string;
  /** Display name */
  name: string;
}

export interface UpdateRoleRequest {
  /** A URL to the JSON Schema for this object. */
  readonly $schema?: string;
  description?: string;
  /** Display name */
  name: string;
}

export interface UserResponse {
  /** A URL to the JSON Schema for this object. */
  readonly $schema?: string;
  /** When the account was registered */
  created_at: Date;
  /** Email address, normalised to lower case */
  email: string;
  /** Unique identifier */
  id: string;
  /** Preferred language, remembered from registration. Outranks Accept-Language. */
  locale?: string;
  /** Display name */
  name: string;
  /** Whether sign-in from an untrusted device needs an emailed code */
  two_factor_enabled: boolean;
}

export interface VerifySessionRequest {
  /** A URL to the JSON Schema for this object. */
  readonly $schema?: string;
  /** The code delivered after signing in */
  code: string;
  /** Email address */
  email: string;
}

/** Request Options for Angular HttpClient requests */
export interface RequestOptions<TResponseType extends 'arraybuffer' | 'blob' | 'json' | 'text'> {
  headers?: HttpHeaders;
  reportProgress?: boolean;
  responseType?: TResponseType;
  withCredentials?: boolean;
  context?: HttpContext;
}
